<%= link_to '物語作成', new_story_path, class: 'btn btn-lg btn-primary' %>
<p>自分の物語</p>
<% @stories.each do |story| %>
  <%= render 'story', story: story %>
<% end %>

<p>みんなの物語</p>
<% @stories.each do |story| %>
  <% if story.published? %>
    <%= render 'story', story: story %>
  <% end %>
<% end %>